import React from "react";

const CypressPage: React.FC = () => {
  return (
    <div>
      <h1>Cypress Testing</h1>
      <p>This page is for Cypress testing.</p>
      <p>
        To run the tests, use the command: <code>npm run cypress</code>
      </p>
      <p>
        Make sure to have the Cypress installed and configured in your project.
      </p>
    </div>
  );
};

export default CypressPage;
